{
  "name": "katalama-botu",
  "version": "1.0.0",
  "description": "CryptoForge — $100 Katlama Botu",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && node src/index.js",
    "dev": "cd backend && node --watch src/index.js"
  }
}
